console.log("bye");

//+git checkhout master+ regresa a la branch principal
// +git remote add origin https://github.com/lileeayee/git-cb-37.git+  (Origin es una variable que cuando sube los cambios lo hace a la URL.  Esto se hace una sola vez para cada repositorio)